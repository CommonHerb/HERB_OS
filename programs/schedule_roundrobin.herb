# Round-robin scheduler
# Selects first ready process for CPU0 (FIFO)

tension proc.schedule_rr priority 10
  match proc in proc.READY select first
  match cpu empty_in proc.CPU0
  emit move proc.schedule proc to cpu
